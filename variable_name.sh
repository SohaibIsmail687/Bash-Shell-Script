#!/bin/bash
# variable_name.sh
_variableName="first variable"
variable2Name="second variable"

name="gaurav"
NAME="saurav"
nAmE="amit"

echo "${name} ${NAME} ${nAmE}"
echo "${_variableName}"

echo "${variable2Name}"
variable_name="vartest"
echo "${variable_name}"

# 3namevariable="myname"
# echo "${3namevariable}"

my-name="gaurav"
echo "${my-name}"
